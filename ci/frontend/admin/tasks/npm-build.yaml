apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: npm-build
spec:
  workspaces:
    - name: source
  steps:
    - name: install-and-build
      image: node:18
      workingDir: $(workspaces.source.path)/frontend/admin
      env:
        - name: NODE_OPTIONS
          value: "--openssl-legacy-provider"
      script: |
        #!/bin/bash
        npm ci
        npm run build
